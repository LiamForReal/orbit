ORBIT TOOL INSRACTIONS!!!


first: 

open docker desktop you must be open and already logged

if you dont have docker desktop yet:
Download Docker Desktop from https://www.docker.com/products/docker-desktop/

Run the installer.

During installation, make sure:

"Use WSL 2 instead of Hyper-V" is selected (recommended for most users). importent!!!

Restart your PC if prompted.

Then open the app and signUp a new docker user


seconed (only if you already had a docker desktop) : 

make sure your docker desktop oparate on windows workspace and not on linox


thired:

you need to change the user name in the ...\orbit\src\Node\build_node_image.bat to yours docker user name


last - recomended installations if you lake of:


python installations:

install python on https://www.python.org/downloads/ if you dont already have 
where python -> copy path without python.exe -> pip install yaml

docker installations:

download docker basic image: mcr.microsoft.com/windows/nanoserver:ltsc2019 from here https://hub.docker.com/r/microsoft/windows-nanoserver
-> excute: ...\orbit\src\Node\build_node_image.bat


vs vcpkg installations:
boost (in the lib uint2048_t,cpp_int, uint256_t)

if you dont have boost lib via vcpkg you can download via .../src/vcpkg_install_boost.bat

gui installations:
where python -> copy path without python.exe -> pip install pyQt6


after all this you all set up!!!
you can use orbit in the dir run_orbit run the server and then one or more clients there is a logs dir to the client backhand you can see. 
thanks for using orbit
